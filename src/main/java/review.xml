<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reviewns">
	<select id="select" parameterType="integer" resultType="review">
		select * from review where rvNum = #{rvNum}
	</select>
	<select id="getTotal" resultType="integer" parameterType="string">
		select count(*) from review 
	</select>
	<select id="get0Total" resultType="integer" parameterType="string">
		select count(*) from review where mvCode = 4
	</select>
	<select id="getSpiTotal" resultType="integer" parameterType="string">
		select count(*) from review where mvCode = 1
	</select>
	<select id="getBatTotal" resultType="integer" parameterType="string">
		select count(*) from review where mvCode = 6
	</select>
	<select id="getMathTotal" resultType="integer" parameterType="string">
		select count(*) from review where mvCode = 16
	</select>
	<select id="getMoonTotal" resultType="integer" parameterType="string">
		select count(*) from review where mvCode = 18
	</select>
	<select id="getSingTotal" resultType="integer" parameterType="string">
		select count(*) from review where mvCode = 9
	</select>
	<select id="reviewList" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="review0List" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where mvCode = 4 order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="reviewSpiList" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where mvCode = 1 order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="reviewBatList" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where mvCode = 6 order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="reviewMathList" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where mvCode = 16 order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="reviewMoonList" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where mvCode = 18 order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<select id="reviewSingList" parameterType="map" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where mvCode = 9 order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
	<insert id="insert" parameterType="review">
		<selectKey keyProperty="rvNum" order="BEFORE" resultType="integer">
			select nvl(max(rvNum), 0) + 1 rvNum from review
		</selectKey>
		insert into review values(#{rvNum}, #{rvTitle}, #{rvContent}, #{id}, #{rvGrade}, 0, sysdate, 'n', #{mvTitle}, #{mvCode}, 0)
	</insert>
	<update id="readCountUpdate" parameterType="integer">
		update review set rvReadCount = rvReadCount +1 where rvNum = #{rvNum}
	</update>
	<update id="delete" parameterType="integer">
		update review set rvDel='y' where rvNum=#{rvNum}
	</update>
	<update id="deleteMaster" parameterType="integer">
		update review set rvDel='m' where rvNum=#{rvNum}
	</update>
	<update id="update">
		update review set rvTitle= #{rvTitle }, rvGrade = #{rvGrade }, rvContent = #{rvContent}, mvCode = #{mvCode}  where rvNum = #{rvNum }
	</update>
	<update id="updateLike" parameterType="integer">
		update review set rvLike = rvLike + 1 where rvNum = #{rvNum }
	</update>
	<update id="mvCodeUpdate">
		update review set mvCode = #{mvCode} where rvNum = #{rvNum }
	</update>
	<select id="mylist" resultType="review">
		select * from (select rowNum rn, a.* from(select * from review where id=#{id} order by rvNum desc)a) where rn between #{startRow} and #{endRow}
	</select>
</mapper>

